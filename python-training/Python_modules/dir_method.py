#we can use dir() method to identify names declaared within the module.
# print("list of functions:\n",dir(str),end=",")
import math
print(dir(math),end=" ")
#NOTE-->TODO--> a variable is treated as global variable when the variable is placed after the keyword "global".
